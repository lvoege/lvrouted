- integrate MAC.ml and Iface.ml
- some kind of authentication scheme. A neighbor will not accept data from non-neighbor addresses,
  but anyone that can spoof an address can send bogus data.
- test for robustness. see what happens when bogus data does get through.
- something to counter marginal links that flip between being associated and not being associated
  relatively quickly.
